// import {

// } from '../actions';

const initialState = {

}

export default function instaAccountReducer(state = initialState, action){
    switch(action.type){
        default:
            return state
    }
}