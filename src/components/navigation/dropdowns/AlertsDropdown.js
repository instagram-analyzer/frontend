import React from 'react';

import { AlertsContainer } from '../navigationStyles.js';

const AlertsDropdown = ({}) => (
    <AlertsContainer>
        <button>
            <i className="fas fa-bell"/>
        </button>
    </AlertsContainer>
)